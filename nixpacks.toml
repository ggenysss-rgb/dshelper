[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm ci", "cd dashboard && npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["cd dashboard && npm run build"]
